# AWS Configuration
AWS_REGION=us-east-2

# AWS Secrets Manager
# Name of the secret in AWS Secrets Manager containing RDS credentials
RDS_SECRET_NAME=rds!cluster-cdec3c33-2379-4799-ba04-00c5deaa3385

# S3 Configuration
# Name of the S3 bucket containing the messy_health_data.csv file
S3_BUCKET_NAME=your-s3-bucket-name
# S3 object key (file path) - defaults to messy_health_data.csv if not set
S3_KEY=messy_health_data.csv

# Local file path for downloaded S3 file
LOCAL_DATA_PATH=./messy_health_data.csv

# RDS Configuration
# AWS RDS Writer Endpoint (e.g., your-db-cluster.cluster-xxxxx.us-east-1.rds.amazonaws.com)
RDS_WRITER_ENDPOINT=database-2.cluster-chs6s6ayoe28.us-east-2.rds.amazonaws.com

# Database Configuration (optional - can also be in Secrets Manager)
# Database name (defaults to 'postgres' if not set)
DB_NAME=postgres
# Database port (defaults to 5432 if not set)
DB_PORT=5432

# AWS Credentials
# These can be set here or via AWS CLI configuration (~/.aws/credentials)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
